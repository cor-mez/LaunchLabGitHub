<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>

	<!-- Required for camera access -->
	<key>NSCameraUsageDescription</key>
	<string>LaunchLab requires camera access to track the golf ball.</string>

	<!-- Required if someday you use microphone (remove if not needed) -->
	<key>NSMicrophoneUsageDescription</key>
	<string>LaunchLab may use audio input in the future.</string>

	<!-- Standard app configuration -->
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
		<key>UISceneConfigurations</key>
		<dict>
			<key>UIWindowSceneSessionRoleApplication</key>
			<array>
				<dict>
					<key>UISceneConfigurationName</key>
					<string>Default Configuration</string>
					<key>UISceneDelegateClassName</key>
					<string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
				</dict>
			</array>
		</dict>
	</dict>

	<!-- Orientation support -->
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>

	<!-- Required for camera pixel buffers -->
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>camera</string>
	</array>

</dict>
</plist>